// src/constants.js

// LocalStorage keys
export const BASIC_INFO_KEY = "cc_basic_info_v1";
export const PREFS_KEY = "cc_prefs_v1";

// Demo access code
export const ACCESS_CODE = "Tay123";

// Default export (assign to variable first to satisfy eslint)
const constants = {
  BASIC_INFO_KEY,
  PREFS_KEY,
  ACCESS_CODE,
};

export default constants;